# Jeffws React App

Jeff's template for a React app

## Development

```bash
# run tests (linting)
npm test

# start dev server (listens on localhost:8080)
npm start

# build (to dist folder)
npm run build
```
